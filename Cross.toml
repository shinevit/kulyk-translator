# Linux x86_64 target - uses custom Docker image with additional build tools
[target.x86_64-unknown-linux-gnu]
image = "x86_64-unknown-linux-gnu:my-edge"

# Linux ARM64 target - uses custom Docker image with additional build tools
[target.aarch64-unknown-linux-gnu]
image = "aarch64-unknown-linux-gnu:my-edge"
